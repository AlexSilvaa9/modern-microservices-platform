server:
  port: 8083
spring:
  datasource:
    url: jdbc:h2:mem:userdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
security:
  jwt:
    secret: changeit_changeit_changeit_changeit_changeit
    expiration-ms: 3600000

